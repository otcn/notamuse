<?php

//fetch files in the current language only
/*
pages::$methods['languageExists'] = function($pages) {

  return $items = $page->children()->visible()->filter(function($child) {
    return $child->content(site()->language()->code())->exists();
  });

});
*/

?>